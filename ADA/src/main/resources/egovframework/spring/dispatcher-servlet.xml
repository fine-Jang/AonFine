<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
           http://www.springframework.org/schema/tx https://www.springframework.org/schema/tx/spring-tx.xsd">

    <!--
      property-placeholder와 component-scan을 이 서블릿(자식) 컨텍스트 하나에만 둔다.
      루트 컨텍스트(context-datasource.xml)와 이중으로 스캔하면 @PostConstruct로 시작하는
      RetentionCleanupScheduler 같은 빈이 두 번 생성되어 정리 배치가 중복 실행될 수 있기 때문이다.
      dataSource 빈은 부모(root) 컨텍스트에 있지만 Spring MVC 부모-자식 컨텍스트 구조상
      이 자식 컨텍스트의 빈들이 문제없이 주입받을 수 있다.
      system-properties-mode=OVERRIDE: ada.worker.apiToken처럼 파일에는 빈 값만 두고 운영에서
      JVM -D 시스템 프로퍼티로 실제 값을 주입하는 키를 허용한다(비밀번호를 소스에 두지 않는 기존 원칙과 동일).
    -->
    <context:property-placeholder location="classpath:ada.properties" system-properties-mode="OVERRIDE" />
    <context:component-scan base-package="com.aonfine.ada" />

    <!-- AnalysisService의 JOB/ATTEMPT/DUMP 원자적 트랜잭션(@Transactional)을 실제로 프록시한다. -->
    <tx:annotation-driven transaction-manager="transactionManager" />

    <mvc:annotation-driven />
    <mvc:resources mapping="/css/**" location="/css/" />
    <mvc:resources mapping="/js/**" location="/js/" />
    <mvc:resources mapping="/images/**" location="/images/" />

    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/**" />
            <mvc:exclude-mapping path="/login.do" />
            <mvc:exclude-mapping path="/loginProcess.do" />
            <mvc:exclude-mapping path="/css/**" />
            <mvc:exclude-mapping path="/js/**" />
            <mvc:exclude-mapping path="/images/**" />
            <!-- Worker 내부 API는 사용자 세션이 아니라 별도 토큰 인증(AdaWorkerAuthInterceptor)만 사용한다. -->
            <mvc:exclude-mapping path="/worker/**" />
            <bean class="com.aonfine.ada.auth.AdaAuthInterceptor" />
        </mvc:interceptor>
        <mvc:interceptor>
            <mvc:mapping path="/worker/**" />
            <bean class="com.aonfine.ada.auth.AdaWorkerAuthInterceptor" />
        </mvc:interceptor>
    </mvc:interceptors>

    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/jsp/" />
        <property name="suffix" value=".jsp" />
    </bean>
</beans>
